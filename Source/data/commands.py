import json, os

'''def setup():
    print('\nSET UP FOR THE GENERIC SOCAIL MEDIA ALGORITHM (WARNING, this will overwrite any previous settings)\n')

    usersDatDir = input('please enter the directory where you want user data to be stored: ')

    x = {
        "usersDatDir": usersDatDir
    }
    # Serializing json/ convert dict to json
    json_object = json.dumps(x, indent=4)
    # Writing to file
    with open("settings.json", "w") as outfile:
        outfile.write(json_object)
    print("completed setup.")'''
