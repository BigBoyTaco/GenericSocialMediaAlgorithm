how to use,

open a cmd window in the same directory as the gsa.exe file. Ex: 'gsa -h' (shows help info)

HELP INFO

commands:
        -n: create a new user
        -c: select potential content to show to user
        -a: add interests to user
        -h: show this help msg